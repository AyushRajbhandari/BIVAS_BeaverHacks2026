Run this by launching the backend in python, then running:
python -m http.server 8080

from here we can hit localhost:8080 to run our app.

Obviously we would like to wrap this up in something prettier.

May or may not add RMP feature.